# 项目演示说明

这份文档用于最终验收时的现场演示，目标是把项目的主要闭环功能快速、稳定地展示出来。

## 演示顺序

### 1. 启动项目

先执行：

```bash
npm install
npm run dev
```

说明应用可以正常启动，界面可以加载。

### 2. 打开工作区

点击左侧 Explorer 的 Open Folder，打开一个真实项目目录。

重点说明：

- 目录树会自动加载
- 最近工作区会记录在 Recent Workspaces 中
- Recent Workspaces 和 Recent Files 都支持折叠、移除和清空

### 3. 文件编辑

打开一个代码文件，展示以下能力：

- Monaco 语法高亮
- 多标签页
- 分栏编辑
- 跳转到具体文件位置
- 保存 / 另存 / 关闭标签

### 4. 终端

打开右侧或底部终端入口，展示：

- 真实 PTY 终端
- 当前工作区 cwd
- 默认 shell 兼容处理
- 多终端标签切换
- 调整终端大小时正常适配

### 5. 外观设置

演示以下任意一种方式：

- 状态栏中的主题切换和字号调整
- 设置面板中的主题与字号
- 快捷键切换主题 / 增大字号 / 缩小字号

说明这些设置会影响编辑器、终端和 diff 视图的整体观感。

### 6. AI Agent

打开 AI 面板，选择一个 OpenAI 兼容提供商后，演示一次带工具调用的交互，例如：

- 读取当前文件
- 搜索代码库
- 让 Agent 修改一个简单文件
- 让 Agent 执行一条终端命令

演示时最好说明：Agent 支持流式输出，并且可读取、编辑、搜索和运行命令。

### 7. Git 与搜索

最后展示：

- Git 状态、Diff、分支和提交入口
- 全局搜索文件内容或文件名

## 建议话术

可以用下面这句话概括项目：

> 这是一个基于 Electron + React + Monaco 的桌面 AI IDE，已经具备文件管理、代码编辑、PTY 终端、Git、搜索、AI Agent、主题和字号统一控制等完整工作台能力。

## 演示前检查

- `npm run build` 已通过
- 目标工作区可以正常打开
- AI Provider 配置已准备好
- 终端默认 shell 在当前机器上可用
